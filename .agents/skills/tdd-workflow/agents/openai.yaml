interface:
  display_name: "Tdd Workflow"
  short_description: "Test-driven development workflow for features, bug fixes, and refactors. Use to force test-first implementation with..."
  default_prompt: "Use Tdd Workflow for this task."
policy:
  allow_implicit_invocation: true
