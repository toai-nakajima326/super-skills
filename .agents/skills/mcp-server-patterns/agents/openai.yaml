interface:
  display_name: "Mcp Server Patterns"
  short_description: "MCP design guidance for tools, resources, transports, validation, and trust boundaries. Use when cataloging MCP..."
  default_prompt: "Use Mcp Server Patterns for this task."
policy:
  allow_implicit_invocation: true
